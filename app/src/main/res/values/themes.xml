<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HealthIconIQ" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/green_default</item>
        <item name="colorPrimaryVariant">@color/green_pressed</item>
        <item name="colorOnPrimary">@android:color/white</item> <!-- Assuming white text on green background -->
    </style>

    <style name="Theme.HealthIconIQ" parent="Base.Theme.HealthIconIQ">
        <!-- Additional theme customizations here -->
    </style>
</resources>
